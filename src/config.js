var config = {
  region: "la2",
  summoner: "summoner",
  summoner_url: process.env.REACT_APP_SUMMONER_URL,
  summoner_rank: process.env.REACT_APP_RANK_URL,
};

//No cambiar o no podra detectar las variables del Array
module.exports = config;
